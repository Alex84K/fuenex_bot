version: "3.9"

services:
  app:
    build: .
    image: fuenex_bun:latest
    container_name: fuenex_bun
    restart: unless-stopped
    ports:
      - "5012:5012"
    env_file:
      - .env
    environment:
      - PORT=5012
      - NODE_ENV=production